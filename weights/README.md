# Released fold weights

The model weights are hosted at
https://huggingface.co/Yu-star/RLMF-m6APred.

Run the following command from the repository root to populate this directory:

```bash
python scripts/download_model.py
```
